<%= form_for(@ding) do |f| %>
  <% if @ding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ding.errors.count, "error") %> prohibited this ding from being saved:</h2>

      <ul>
      <% @ding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :zeug %><br>
    <%= f.text_area :zeug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
